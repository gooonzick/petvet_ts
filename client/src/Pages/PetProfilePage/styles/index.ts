import { SxProps, Theme } from '@mui/material';

// eslint-disable-next-line import/prefer-default-export
export const pageWraperBoxStyle: SxProps<Theme> = {
  padding: '1rem',
  position: 'relative',
};
