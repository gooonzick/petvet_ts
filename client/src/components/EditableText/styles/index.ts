import { CSSProperties } from 'react';

import { SxProps, Theme } from '@mui/material';

export const iconContainer: CSSProperties = { display: 'flex' };
export const textField: SxProps<Theme> = { width: 'max-content' };
